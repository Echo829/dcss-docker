Dockerfile for building a Dungoen Crawl Stone Soup server (https://crawl.develz.org/).

To run, you'll need to expose ports 8080 and 8443. Can be run as is but you may want to mount config.py and base.yaml to modify settings and game modes. See docker-compose file for example.